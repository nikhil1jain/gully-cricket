let initialState = {};

const landingPageReducer = (state = initialState, action: any) => {
  switch (action.type) {
    default:
      return {
        ...state,
      };
  }
};

export default landingPageReducer;
